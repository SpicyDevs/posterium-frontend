# Static files must NOT be caught by the SPA fallback.
# List them explicitly BEFORE the SPA catch-all rule.
# Cloudflare Pages processes rules top-to-bottom; first match wins.

/sitemap.xml    /sitemap.xml    200
/robots.txt     /robots.txt     200
/og-image.jpg   /og-image.jpg   200

# SPA fallback — serves index.html for all other routes (e.g. /build)
/*              /index.html     200
